[pytest]
testpaths = tests app
asyncio_mode = auto
# 每个测试用自己的事件循环 —— async engine 的连接池绑定在创建它的循环上，
# 跨循环复用会报 "Event loop is closed"（见 docs/07-bug-log.md BUG-005）
asyncio_default_fixture_loop_scope = function
# 把 docstring 里的示例也当测试跑 —— 文档写错会被抓到，不会烂掉
addopts = --doctest-modules
