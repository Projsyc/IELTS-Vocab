fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
greenlet>=3.0.0          # SQLAlchemy async 的隐式依赖，缺了会报 "the greenlet library is required"
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0            # 直接用，不经 passlib —— passlib 1.7.4 读不了 bcrypt 4.1+ 的版本号（BUG-007）
python-multipart>=0.0.9
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.24.0

# —— seed 脚本用 ——
edge-tts>=6.1.0            # 音频兜底合成（24% 的词 dictionaryapi.dev 没有音频）
